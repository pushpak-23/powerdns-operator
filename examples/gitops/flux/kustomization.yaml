apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: powerdns-platform
  namespace: flux-system
spec:
  interval: 10m
  path: ./manifests/production
  prune: true
  sourceRef:
    kind: GitRepository
    name: powerdns-platform
  targetNamespace: dns-platform
